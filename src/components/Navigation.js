import React from 'react';
import './Navigation.css';

const Navigation = () => {
  return (
    <nav className="bottom-navigation">
      {/* This component can be used for additional navigation elements if needed */}
    </nav>
  );
};

export default Navigation;
